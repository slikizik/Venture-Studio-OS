# Acceptance Criteria

Each requirement below is blocking unless explicitly deferred by an approved decision record.

## PRJ-001 — Create a project from a built-in or custom template

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 02.
- `TEST-PRJ-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## PRJ-002 — Edit project metadata and Project Brain

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 02.
- `TEST-PRJ-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## PRJ-003 — Archive and restore a project without data loss

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 02.
- `TEST-PRJ-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## PRJ-004 — Search sort and filter projects

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 02.
- `TEST-PRJ-004` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## PRJ-005 — Calculate and display project phase progress health risks and blockers

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 07.
- `TEST-PRJ-005` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## DEL-001 — Create edit reorder archive and restore deliverables

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 03.
- `TEST-DEL-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## DEL-002 — Support validated parent-child hierarchy

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 03.
- `TEST-DEL-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## DEL-003 — Store status owner due date dependencies criteria and versions

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 03.
- `TEST-DEL-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## WPK-001 — Create work packet linked to a project and optional deliverable

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 04.
- `TEST-WPK-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## WPK-002 — Store complete work packet specification evidence status and assignee

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 04.
- `TEST-WPK-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## WPK-003 — Submit versioned work for review

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 04.
- `TEST-WPK-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## REV-001 — Create criterion-based review records

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 05.
- `TEST-REV-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## REV-002 — Approve reject or request revision under transition rules

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 05.
- `TEST-REV-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## REV-003 — Preserve immutable review comments and history

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 05.
- `TEST-REV-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## TPL-001 — Load and validate built-in project templates

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 06.
- `TEST-TPL-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## TPL-002 — Create stages deliverables brain sections and gates from a template

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 06.
- `TEST-TPL-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## TPL-003 — Create and version custom templates

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 06.
- `TEST-TPL-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## GOV-001 — Create immutable decision records

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 05.
- `TEST-GOV-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## GOV-002 — Record and manage risks

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 05.
- `TEST-GOV-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## GOV-003 — Record project versions and change summaries

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 03.
- `TEST-GOV-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## DSH-001 — Display portfolio dashboard metrics

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 07.
- `TEST-DSH-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## DSH-002 — Display project dashboard

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 07.
- `TEST-DSH-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## DSH-003 — Display pending reviews blockers risks recent decisions and progress

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 07.
- `TEST-DSH-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## AGT-001 — Store generic agent records capabilities provider model autonomy and status

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 02.
- `TEST-AGT-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## AGT-002 — Maintain ordered execution queue with dependencies and evidence

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 04.
- `TEST-AGT-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## AGT-003 — Record append-only agent activity and outcomes

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 04.
- `TEST-AGT-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## EXC-001 — Classify recoverable assumptions direction-required and critical issues

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 05.
- `TEST-EXC-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## EXC-002 — Create formal direction requests

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 05.
- `TEST-EXC-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## EXC-003 — Continue unaffected work while an item is blocked

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 05.
- `TEST-EXC-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## EXC-004 — Record owner decisions and resume blocked work

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 05.
- `TEST-EXC-004` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## VER-001 — Present understandable project version labels

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 03.
- `TEST-VER-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## VER-002 — Record official development test and released versions

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 03.
- `TEST-VER-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## VER-003 — Record comparison approval merge readiness and release status

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 09.
- `TEST-VER-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## ENV-001 — Register isolated software development environments

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 09.
- `TEST-ENV-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## ENV-002 — Record branch worktree port database storage logs and status

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 09.
- `TEST-ENV-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## ENV-003 — Prevent runtime data sharing between isolated environments

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 08.
- `TEST-ENV-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## ENV-004 — Display visible environment identity in non-production builds

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 07.
- `TEST-ENV-004` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## DAT-001 — Export a complete project as a versioned package

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 08.
- `TEST-DAT-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## DAT-002 — Import a compatible project package transactionally

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 08.
- `TEST-DAT-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## DAT-003 — Reject invalid or unsupported import packages safely

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 08.
- `TEST-DAT-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## DAT-004 — Create backups before destructive migrations and imports

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 01.
- `TEST-DAT-004` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## DAT-005 — Restore a verified backup with integrity checks

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 08.
- `TEST-DAT-005` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## ATT-001 — Attach files links notes test results and artifacts

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 04.
- `TEST-ATT-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## ATT-002 — Sanitize filenames validate size and preserve checksums

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 04.
- `TEST-ATT-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## AUD-001 — Record required append-only audit events

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 02.
- `TEST-AUD-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## NFR-001 — Application must run locally on Windows 11 and Ubuntu LTS

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 01.
- `TEST-NFR-001` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## NFR-002 — All persisted data must survive application restart

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 08.
- `TEST-NFR-002` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## NFR-003 — TypeScript strict mode must pass without errors

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 01.
- `TEST-NFR-003` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## NFR-004 — Core pages must meet WCAG 2.1 AA keyboard and contrast expectations

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 08.
- `TEST-NFR-004` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## NFR-005 — Primary list and dashboard actions should respond within two seconds at MVP data volume

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 08.
- `TEST-NFR-005` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## NFR-006 — Rendered Markdown and user text must be sanitized

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 08.
- `TEST-NFR-006` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## NFR-007 — Production release must have no unresolved critical dependency vulnerabilities

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 09.
- `TEST-NFR-007` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## NFR-008 — All dates must be stored in UTC and displayed in owner timezone

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 02.
- `TEST-NFR-008` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## NFR-009 — Critical records and decisions must be append-only or versioned

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 05.
- `TEST-NFR-009` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.

## NFR-010 — Every completed requirement must link to passing evidence

- The implemented behaviour matches the normative data, workflow, screen, and architecture contracts referenced by Phase 08.
- `TEST-NFR-010` passes and produces retained evidence.
- Invalid input is rejected without corrupting existing data.
- Behaviour persists after restart where persistence applies.
- Relevant activity and traceability records are updated.
## INT-001 — Capture and approve a plain-language Project Intent Brief with outcome audience constraints success measures commercial target and priorities

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 2.
- `TEST-INT-001` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## INT-002 — Create and approve a project benchmark brief with target dimensions differentiators and intentional omissions

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 2.
- `TEST-INT-002` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## DSG-001 — Trace approved intent into requirements and acceptance criteria without silently changing owner intent

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 2.
- `TEST-DSG-001` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## DSG-002 — Map requirements to deliverables dependencies and executable work packets with explicit exclusions and evidence expectations

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 4.
- `TEST-DSG-002` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## QLT-001 — Create and version a project Quality Profile with project-type quality dimensions and target levels

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 6.
- `TEST-QLT-001` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## QLT-002 — Evaluate work packet deliverable stage and release quality gates using retained evidence

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 5.
- `TEST-QLT-002` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## QLT-003 — Record benchmark gaps and show whether mandatory commercial targets are met or explicitly accepted

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 7.
- `TEST-QLT-003` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## QLT-004 — Distinguish technically ready from commercially ready release states

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 9.
- `TEST-QLT-004` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## LRN-001 — Capture learning records from defects feedback tests benchmarks usability commercial insights agent failures and retrospectives

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 5.
- `TEST-LRN-001` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## LRN-002 — Convert assessed learning into controlled change requests requirements or backlog without silently expanding scope

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 5.
- `TEST-LRN-002` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## AUT-001 — Calculate autonomy rate and first-pass acceptance from auditable execution and review records

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 7.
- `TEST-AUT-001` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## AUT-002 — Record and display owner attention time without suppressing required escalations

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 7.
- `TEST-AUT-002` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## AUT-003 — Record escalation quality and identify unnecessary escalation events

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 7.
- `TEST-AUT-003` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## AUT-004 — Calculate autonomous recovery rate from classified recoverable execution incidents

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 7.
- `TEST-AUT-004` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

## SEVL-001 — Complete one persisted end-to-end Smallest Executable VSO Loop from intent through learning with evidence and review

- The implemented behaviour matches the Product Doctrine and normative contracts assigned to Phase 5.
- `TEST-SEVL-001` passes and produces retained evidence.
- Invalid input or missing evidence fails safely without inventing completion.
- Relevant audit, traceability, and decision records are updated.
- Product-intent, commercial-quality, or owner-attention claims are derived from stored evidence rather than agent assertion.

